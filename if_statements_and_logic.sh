#!/bin/bash
#
# Inside of IF Statements the following is true:
# && = AND and lets you join numerous tests together
# || = OR
# ! = NOT ie:
#	[ ! -e "/usr/bin" ] # Checks path does NOT exist.
#	[ -e "/usr/bin" ] # Checks path DOES exist.
